import React from "react";
const NavBar = ()=>{
    
    return(
        <div className="gray">
            <nav className="between"> 
                <p className="welcon" id="User">Welcome User</p>
                <button className="logout">Logout</button>
            </nav>

        </div>
    );
}
export default NavBar;